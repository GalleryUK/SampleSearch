#include <Sound.h>#include <SoundComponents.h>#include <SoundInput.h>	/* unsigned fixed-point number *//*typedef unsigned long UnsignedFixed;*/typedef struct ConversionRec {	SoundConverter theSC;	unsigned long inputBufferFrames;	unsigned long inputBufferBytes;	unsigned long outputBytes;			SoundComponentData sourceComponent;	unsigned long sourceFrames;	unsigned long sourceBytes;	unsigned long sourceDataOffset;		SoundComponentData  destComponent;	unsigned long destFrames;	unsigned long destDataOffset;	unsigned long destBytes;		OSErr convertError;} ConversionRec, *ConversionRecPtr;OSErr fillInConvRec (ConversionRec * myCR, UnsignedFixed SRin, UnsignedFixed SRout, Boolean doSR, short BITSin, short BITSout, Boolean doBITS, short CHANSin, short CHANSout, Boolean doCHANS);OSErr prepareRawConversion (ConversionRec * myCR);OSErr endRawConversion (ConversionRec * myCR);OSErr convertBuffer (ConversionRec * myCR);/* fill in conv rec	prepareRawConversion	loop  convert buffer 	end raw conversion*/