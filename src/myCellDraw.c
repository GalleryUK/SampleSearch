#include "SampleSearch.h"#include "Globals.h"typedef struct listFontRec {Str255 fontName;short fontSize;} listFontRec, *listFontRecPtr;extern listFontRec  theFontRec;void myLSetCell(Ptr theData, short dataLength, Cell theCell, ListHandle myList);void myLSetCell(Ptr theData, short dataLength, Cell theCell, ListHandle myList){	short fontNum;		GrafPtr savePort;	GetPort (&savePort);						SetPort(myWindow);		color.red = 0xFFFF;	color.blue = 0xFFFF;	color.green = 0xFFFF;	RGBBackColor(&color);	GetFNum(theFontRec.fontName,&fontNum);	TextFont(fontNum);						/** font for list - chicago **/	TextSize(theFontRec.fontSize);		LSetCell(theData, dataLength, theCell, myList);		color.red = 0;	color.blue = 0;	color.green = 0;	RGBForeColor(&color);		//TextFont (0);	//TextSize (12);	SetPort (savePort);}